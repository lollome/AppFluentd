apiVersion: v1
kind: Service
metadata:
  labels:
    app: spamfluentd
    release: spamfluentd-1
  name: s-spamfluentd
  # namespace: kube-spam
spec:
  type: NodePort
  ports:
  - name: spamfluentd-test-http
    port: 8080
    protocol: TCP
    targetPort: 8080 # porta interna
  # - name: gestioneservizi-test-https
  #   port: 443
  #   protocol: TCP
  #   targetPort: 443
  selector:
    app: spamfluentd
    release: spamfluentd-1
